/* eslint-disable */
// @ts-nocheck
import { RequestFilters } from "./RequestFilters";
import { assetTypes } from "./assetTypes";
import { countries } from "./countries";

const defaultRequestFilters: RequestFilters = { countries: [], assetTypes: [], budgetRange: [0, 50000] };
export { defaultRequestFilters };
